// const { nextI18NextRewrites } = require('next-i18next/rewrites')

const localeSubpaths = {
  en: 'en',
  ja: 'ja'
}

module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'ja']
  }
}
